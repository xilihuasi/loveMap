<!--index.wxml-->
<view class="container">
  <map class="map" 
  	   markers="{{markers}}" 
  	   latitude="{{latitude}}"
  	   longitude="{{longitude}}"
  	   include-points="{{includePoints}}"
  	   bindtap="bindtap"
  	   polyline="{{polyline}}"
  	   bindmarkertap="toDetail"></map>
</view>